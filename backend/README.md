# backend/

API REST en FastAPI (Python), siguiendo Clean Architecture:
`domain/`, `application/`, `infrastructure/`, `api/`.

Vacío en la Etapa 1 — se implementa a partir de la Etapa 6 (API REST base),
con autenticación desde la Etapa 4.
