# ai/

Módulo SIEGEM Lab — IA gemológica (PyTorch, OpenCV, YOLO).

- `models/` — Modelos entrenados (no versionados en Git, ver .gitignore).
- `datasets/` — Datasets de inclusiones, capturas y calibraciones.

Vacío en la Etapa 1 — se implementa en la Etapa 13.
