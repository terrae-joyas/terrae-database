# blockchain/

Smart contracts EmeraldChain (Solidity) sobre Polygon.

- `contracts/` — Contratos Solidity (ej. SiegemLabAlpha).
- `scripts/` — Scripts de despliegue y verificación.

Vacío en la Etapa 1 — se implementa en la Etapa 12.
